<%# app/views/products/index.erb %>
<h1>Products</h1>

<div class="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product_path(product.id) %>
    </div>
  <% end %>
</div>